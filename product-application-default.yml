spring:
  application:
    name: product-application
server:
  port: 8091
eureka:
  client:
    service-url:
      defaultZone: ${eureka_url:http://localhost:8761/eureka}
  instance:
    hostname: localhost
    prefer-ip-address: true
